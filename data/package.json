{
  "name": "data",
  "version": "1.0.0",
  "description": "",
  "main": "convert-csv-to-json.js",
  "scripts": {
    "convert": "for f in ${DATA_SETS} ; do INPUT_FILE=$f.csv OUTPUT_FILE=$f.json node convert-csv-to-json.js ; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csvjson": "^5.1.0"
  }
}
